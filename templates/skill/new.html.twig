{% extends app.request.xmlHttpRequest ? 'base_ajax.html.twig' : 'base.html.twig' %}

{% block title %}{{ 'skill.new.title'|trans }} | {{ parent() }}{% endblock %}

{% block body %}
	{% if app.request.xmlHttpRequest %}
	<div class="modal-header">
		<h3 class="modal-title">{{ 'skill.new.heading'|trans }}</h3>
		<button type="button" class="close" data-dismiss="modal" aria-label="{{ 'global.action.close' }}">
			<span aria-hidden="true">×</span>
		</button>
	</div>
	<div class="modal-body">
	{% else %}
	<h2>{{ 'skill.new.heading'|trans }}</h2>
	{% endif %}

	{% include 'skill/form.html.twig' with {'form': form} %}

	{% if not app.request.isXmlHttpRequest() %}
	<a href="{{ path('skill_index') }}" class="btn btn-light float-right"><i class="fa fa-list"></i></a>
	{% endif %}

	{% if app.request.isXmlHttpRequest() %}
	</div>
	{% endif %}
{% endblock %}

{% block javascripts %}
		<script src="{{ asset('js/collection_adder.js') }}"></script>
		<script src="{{ asset('js/entity_adder.js') }}"></script>
{% endblock %}